services:
  postgres-test:
    image: postgres:15
    container_name: recipies_me_test_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: recipies_me_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
